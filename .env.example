OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
RELAYN_ORG_ID=
RELAYN_WORKFLOW_ID=
RELAYN_SERVICES_URL=http://localhost:8001
CAL_API_KEY=
CAL_EVENT_TYPE_ID=
CAL_TIMEZONE=Asia/Kathmandu
# Shared with relayn_gateway, which holds the same value in the AGENT_SECRET_*
# variable this service is registered under (agent_services.hmac_secret_env).
# Empty refuses every /v1/reply -- fail closed. Not needed to run the service
# on its own, only to be reachable through the gateway.
GATEWAY_HMAC_SECRET=
